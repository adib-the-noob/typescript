enum OperatingSystems {
    MACOS = 'macOS',
    WINDOWS = 'Windows',
    LINUX = 'Linux',
}

const selectedOS = OperatingSystems.MACOS;
console.log(`Selected OS: ${selectedOS}`); // Output: Selected OS: 0
